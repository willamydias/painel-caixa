name: painel
services:
  painel-imobiliario:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: painel-imobiliario-app
    restart: unless-stopped
    ports:
      - "3050:3050"
    volumes:
      - ../../webscrap/data/ativos:/app/public/data/ativos
      - ../../webscrap/data/reports:/app/public/data/reports
    env_file:
      - .env
    environment:
      - PORT=3050
      - HOSTNAME=0.0.0.0
      - NODE_ENV=production
      - DATABASE_URL=postgresql://postgres:L3il0jus_P4ssW0rd!2026@supabase-db:5432/postgres
      - NEXT_PUBLIC_SUPABASE_URL=http://localhost:8000
      - SUPABASE_INTERNAL_URL=http://supabase-rest:3000
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJyb2xlIjoiYW5vbiIsImlzcyI6InN1cGFiYXNlIiwiaWF0IjoxNzAwMDAwMDAwLCJleHAiOjIwNTAwMDAwMDB9.bl88-wO2IxIfDw-d2LT7Xx6T5g_oY216PU4hPCJZouw
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - default
      - supabase_default

networks:
  default:
  supabase_default:
    name: supabase_default
    external: true
